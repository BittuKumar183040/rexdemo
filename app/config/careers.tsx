import { CARRERS } from "../about/Carrers";

/* {
  title: string, (title for the job application)
  location: string,
  type: string,
  link: string, (link for external portal like google form, linkdIn...)
} */

export const carrers_list: CARRERS[] = [

]